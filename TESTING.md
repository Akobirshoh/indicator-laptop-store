# INDICATOR Laptop Store - Документация тестирования

## 📊 Статус тестирования

![Backend Tests](https://github.com/Akobirshoh/indicator-laptop-store/actions/workflows/backend-tests.yml/badge.svg)
![Frontend Tests](https://github.com/Akobirshoh/indicator-laptop-store/actions/workflows/frontend-tests.yml/badge.svg)
![Code Quality](https://github.com/Akobirshoh/indicator-laptop-store/actions/workflows/code-quality.yml/badge.svg)

## 🧪 Локальное тестирование

### Бэкенд тесты

**Установка зависимостей для тестирования:**
```bash
cd indicator-laptop-store
pip install -r requirements.txt
pip install pytest pytest-asyncio httpx
```

**Запуск всех тестов:**
```bash
pytest tests/ -v
```

**Запуск тестов с покрытием:**
```bash
pytest tests/ --cov=app --cov-report=html
```

**Запуск конкретного файла тестов:**
```bash
pytest tests/test_auth.py -v
pytest tests/test_items.py -v
pytest tests/test_orders.py -v
```

### Фронтенд тесты

**Установка зависимостей:**
```bash
cd frontend
npm install
```

**Запуск тестов (если есть):**
```bash
npm test
```

**Сборка продакшена:**
```bash
npm run build
```

## 📋 Покрытие тестами

### Бэкенд (Backend)
- ✅ **Authentication** (`test_auth.py`)
  - Регистрация пользователя
  - Вход в систему
  - Проверка неверных учетных данных
  - Health check

- ✅ **Items** (`test_items.py`)
  - Получение списка товаров
  - Пагинация
  - Поиск товаров
  - Фильтрация по категориям
  - Получение категорий

- ✅ **Orders** (`test_orders.py`)
  - Получение заказов
  - Создание заказов
  - История заказов
  - Авторизация для заказов

### Фронтенд (Frontend)
- ✅ Компиляция React приложения
- ✅ Сборка для продакшена
- ✅ Проверка синтаксиса

## 🔄 GitHub Actions Workflows

### 1. Backend Tests (`backend-tests.yml`)
- Запускается на: `push` и `pull_request` в `main` и `develop`
- Сервис: PostgreSQL 15
- Python версия: 3.11
- Результаты: Coverage report в Codecov

### 2. Frontend Tests (`frontend-tests.yml`)
- Запускается на: `push` и `pull_request` в `main` и `develop`
- Node версии: 18.x, 20.x
- Операции: npm test, npm build
- Результаты: Coverage report в Codecov

### 3. Code Quality (`code-quality.yml`)
- Запускается на: `push` и `pull_request` в `main` и `develop`
- Инструменты: flake8, pylint, black
- Проверка: синтаксис Python, форматирование кода

## 📈 Continuous Integration

При каждом push или pull request автоматически:
1. ✅ Проходят backend тесты (pytest)
2. ✅ Проходят frontend тесты (npm test)
3. ✅ Проверяется качество кода (flake8, black)
4. ✅ Загружается coverage в Codecov
5. ✅ Собирается production build

## 🎯 Требования к коду

- ✅ Все тесты должны проходить
- ✅ Минимум 70% code coverage
- ✅ Нет синтаксических ошибок (flake8)
- ✅ Форматирование в соответствии с black
- ✅ Успешная сборка фронтенда

## 📚 Структура тестов

```
tests/
├── __init__.py
├── test_auth.py          # Тесты аутентификации
├── test_items.py         # Тесты товаров
├── test_orders.py        # Тесты заказов
└── conftest.py           # Конфигурация pytest
```

## 🚀 Развертывание

Все workflow-ы должны пройти перед merge в main:
1. Все тесты зелены ✅
2. Code quality проверки пройдены ✅
3. Coverage выше 70% ✅
4. Build успешен ✅

## 📞 Помощь и контакты

При возникновении проблем:
1. Проверьте локальные тесты: `pytest tests/ -v`
2. Проверьте лог GitHub Actions
3. Убедитесь что все зависимости установлены
4. Проверьте версии Python и Node.js

---

**Автор:** Akobirshoh  
**Репозиторий:** https://github.com/Akobirshoh/indicator-laptop-store
