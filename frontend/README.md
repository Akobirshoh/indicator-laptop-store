# 🛍️ INDICATOR Frontend

Современный фронтенд для магазина ноутбуков и аксессуаров на React.

## 🎨 Особенности

- ✅ **Зелёный логотип** (#22c55e) на красивой обёртке
- 🔐 **Аутентификация** - Вход и регистрация пользователей
- 📱 **Каталог товаров** - Поиск, фильтрация по категориям
- 🛒 **Корзина покупок** - Управление количеством товаров
- 📦 **Оформление заказов** - С информацией о доставке
- 📱 **Адаптивный дизайн** - Работает на мобильных устройствах

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Запуск приложения

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

### 3. Убедитесь, что бэкенд работает

```bash
# В другом терминале
cd ..
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
```

Бэкенд должен быть доступен на `http://localhost:8000`

## 📂 Структура проекта

```
frontend/
├── public/
│   ├── index.html
│   └── index.css
├── src/
│   ├── components/
│   │   ├── Header.js       # Заголовок с логотипом и навигацией
│   │   ├── Header.css
│   │   ├── ProductList.js  # Каталог товаров
│   │   ├── ProductList.css
│   │   ├── Cart.js         # Корзина покупок
│   │   └── Cart.css
│   ├── App.js
│   ├── App.css
│   ├── api.js              # API интеграция
│   ├── config.js           # Конфигурация
│   ├── index.js
│   └── index.css
├── package.json
└── README.md
```

## 🎯 Основные компоненты

### Header (Заголовок)
- **Логотип** INDICATOR зелёного цвета (#22c55e)
- **Навигация** между каталогом и корзиной
- **Аутентификация** - кнопка входа/выхода

### ProductList (Каталог)
- Отображение товаров в виде карточек
- Поиск по названию товаров
- Фильтрация по категориям
- Добавление товаров в корзину

### Cart (Корзина)
- Просмотр товаров в корзине
- Управление количеством товаров
- Форма доставки
- Оформление заказа

## 🔧 API Endpoints

Приложение использует следующие эндпоинты бэкенда:

- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /items` - Список товаров
- `GET /categories` - Список категорий
- `POST /orders` - Создание заказа

## 💡 Примечания

- При отсутствии интернета используются **моковые данные**
- JWT токены сохраняются в `localStorage`
- Корзина сохраняется в `localStorage` и восстанавливается при перезагрузке

## 📝 Лицензия

MIT
